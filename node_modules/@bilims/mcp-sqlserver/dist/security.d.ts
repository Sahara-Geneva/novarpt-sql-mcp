export declare class QueryValidator {
    private static readonly ALLOWED_STATEMENTS;
    private static readonly FORBIDDEN_KEYWORDS;
    static validateQuery(query: string): {
        isValid: boolean;
        error?: string;
    };
    private static containsSqlInjectionPatterns;
    static sanitizeQuery(query: string): string;
    static addRowLimit(query: string, maxRows: number): string;
}
//# sourceMappingURL=security.d.ts.map